{
  "//": "Merge into ~/.gemini/settings.json (or .gemini/settings.json in a project). Replace the URL host for production and export MCP_API_KEY in your shell.",
  "mcpServers": {
    "protocol-strategist": {
      "httpUrl": "http://localhost:3000/api/mcp",
      "headers": {
        "Authorization": "Bearer $MCP_API_KEY"
      },
      "timeout": 300000,
      "trust": false
    }
  }
}
